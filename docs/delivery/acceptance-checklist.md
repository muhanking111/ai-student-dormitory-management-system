# Route A 本地演示验收清单

> 此文件是操作员验收模板（UNSIGNED），空勾选不代表自动化失败，也不代表人工已签署。本轮自动化执行结果及未运行项见 [验证记录](../../plan/ai-verification.md)。用户/学校人工签署留待现场验收；Agent 不代签。

## 环境

- [ ] 从新 PowerShell 终端进入仓库根目录。
- [ ] `.env` 来自 `.env.example`，所有密码占位值已替换且文件未被 Git 跟踪。
- [ ] Java 21、Maven、Node.js、npm、Docker Desktop 可用。
- [ ] `5174/8081/3307/6380` 无未知监听，原有服务不会被接管。

## 启动与健康

- [ ] `demo-readonly` 从停止或 reset 状态启动成功。
- [ ] 前端首页、入口脚本、后端 health、登录、会话恢复和业务读取均为 `200`。
- [ ] provider 为 `fake`，AI 写执行为 `false`。
- [ ] audit、prompt、tool catalog、budget 均为 `READY`。

## 10-15 分钟流程

- [ ] 登录、刷新和菜单 RBAC。
- [ ] Dashboard 指标、趋势、风险概览和待办。
- [ ] AI 助手流式输出、停止/重试、引用、无可靠来源和撤权状态。
- [ ] 维修详情、AI 分诊、提案预览和审批入口。
- [ ] 公告要点、纯文本草稿、检查、diff 和审批入口。
- [ ] 风险规则、案例详情、人工处置和模型降级。
- [ ] 审批的 diff、step-up、批准/拒绝/失效和 execution 结果。
- [ ] 审计时间线、脱敏内容和正文权限拒绝。
- [ ] 普通业务 CRUD/状态流转；AI 不可用时人工流程仍可使用。

## 视口与异常状态

- [ ] `1366x768` 桌面关键页面无横向溢出、遮挡或不可达操作。
- [ ] `390x844` 移动关键页面无全页横向溢出，触控目标可用。
- [ ] AI 关闭/降级不会显示静态成功。
- [ ] 权限不足不泄露未授权页面或对象正文。
- [ ] 引用撤销后不回放旧正文。
- [ ] 审批过期/失效时批准按钮被禁用。
- [ ] 后端不可用时显示错误或重试状态，不把空数据冒充成功。

## 审批模式

- [ ] 停止只读模式后启动 `demo-approval`。
- [ ] 使用独立 approval demo 数据库、Redis DB 13 和固定 prefix。
- [ ] provider 仍为 `fake`，AI 写执行为 `true`。
- [ ] 写操作仍经过权限、提案、diff、人工审批、step-up、审计和现有 Service。

## 停止、reset 与复现

- [ ] 错误 reset 令牌被拒绝，运行环境和数据未被改动。
- [ ] stop 只停止当前 state 所属进程和 Compose 服务。
- [ ] 正确 reset 只操作固定演示数据库和 Redis prefix。
- [ ] reset 后可以重新启动并再次通过健康检查。
- [ ] 停止后 `5174/8081/3307/6380` 全部关闭，其他本地服务保持不变。

## 结论边界

- [ ] 演示数据为合成 fixture，没有录入真实学生或学校数据。
- [ ] 未调用真实模型供应商、未部署、未启用生产开关。
- [ ] Route B 与全部生产外部门保持 `NOT RUN / NOT APPROVED`。
