# 项目执行规则

本文件是仓库级约束。所有后续 Agent 在本项目内工作时，除遵守更高优先级指令外，还必须遵守以下规则。

## 计划与设计驱动

1. 开工前按 `plan/README.md` 确认事实优先级，并阅读与任务相关的 `plan/ai-master-plan.md`、数据/安全合同、`plan/development-prompts.md` 及对应 `design/ai-prototypes/*.png`。
2. 9 张最终 PNG 是唯一视觉合同；页面修改前必须使用 `view_image` 查看对应原图，不调用 Figma，不重新生成或替换原型资产。
3. 当前源码、OpenAPI、schema 和新鲜测试/运行证据优先于旧进度文字；不得仅依据文档中的 `completed` 声明跳过复核。

## 阶段状态实时更新

1. 每个阶段开始前，必须在当前活动任务计划中将该阶段标记为 `in_progress`，并写明目标、涉及文件、验收方式和阻塞条件。
2. 每个阶段完成后，必须在同一工作会话内立即把状态更新为 `completed`，同时把实际执行的验证命令和结果写入活动 `.planning/<task>/progress.md`；禁止等到所有阶段结束后批量补写状态。
3. 阶段只有在交付物已落盘且对应验证通过后才能标记为 `completed`。验证失败、缺少环境或需要外部批准时，必须保持 `in_progress` 或标记为 `blocked` / `NOT RUN`，并记录原因、剩余风险和解阻条件。
4. 阶段状态变化若影响现行范围、能力或发布门，必须同步更新 `plan/ai-master-plan.md`；精确测试数、覆盖率、制品哈希和 `NOT RUN` 证据只更新 `plan/ai-verification.md`。
5. 每次状态更新后必须重新对照代码、配置、原型和运行结果，确保计划状态是事实记录，而不是完成意愿。

## 实现与验证

1. 先复现或补测试，再做最小范围实现；不得删除断言、降低覆盖率门或弱化 CSRF、Origin、RBAC、审计、PII、ACL、审批及状态机约束来换取通过。
2. 页面和共用组件遵循 `plan/development-prompts.md` 的逐页提示词，并运行受影响的单测、类型检查、构建、E2E 和六视口视觉合同。
3. 未经用户明确批准，不执行部署、推送、生产开关启用、真实供应商调用或其他外部写操作。
