# 安全策略

## 支持范围

本仓库当前只支持默认分支和最新公开 Release Candidate。历史提交、个人分支、第三方 fork，以及 Route B 中尚未执行的预发布或生产部署不在支持范围内。

当前公开版本是本地演示与工程候选，不代表真实供应商、生产基础设施、灾备、压测、灰度或生产放行已经完成。生产边界以 [AI 安全契约](plan/ai-security.md) 和 [AI 验证与验收](plan/ai-verification.md) 为准。

## 报告漏洞

请使用 GitHub 仓库的 **Security -> Report a vulnerability** 私下提交安全问题。不要在公开 Issue、Discussion、截图、日志或演示数据中披露漏洞细节、凭据、Cookie、真实学生信息或其他个人数据。

报告请尽量包含：

- 受影响的 commit、tag、接口或页面；
- 最小复现步骤、影响范围和必要的脱敏证据；
- 是否需要特定角色、CSRF/Origin 条件、审批状态或 AI 能力开关；
- 已知的缓解方式，但不要在未经授权的真实系统上继续验证。

维护者会先确认是否已收到报告，再根据可复现性和影响确定修复与披露安排。请在协调披露完成前保持细节私密。

## 安全边界

- 不接受真实密码、Token、私钥、数据库 dump、真实 PII 或生产日志作为复现材料。
- 不得通过关闭 RBAC、CSRF/Origin、SSE 鉴权、引用 ACL、PII、step-up、审批、审计或状态机约束来规避问题。
- 本地演示固定使用隔离数据库、专用 Redis DB/prefix 和 Fake provider；真实供应商与生产资源需要单独授权。
- 发现疑似已泄露凭据时，应先撤销或轮换凭据，再评估 Git 历史和公开制品影响。

## 依赖与供应链

安全更新应保留 lock file，并通过受影响的 lint、测试、覆盖率、类型检查、构建、E2E、依赖审计和制品哈希门。未解释的高危依赖项不得进入公开稳定结论。
