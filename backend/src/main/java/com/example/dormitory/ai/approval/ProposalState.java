package com.example.dormitory.ai.approval;

public enum ProposalState {
    DRAFT,
    PENDING_APPROVAL,
    APPROVED,
    EXECUTING,
    NEEDS_REVIEW,
    SUCCEEDED,
    REJECTED,
    EXPIRED,
    STALE,
    CANCELLED,
    FAILED
}
